% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_graphGeneration.R
\name{generateQuantGraphs}
\alias{generateQuantGraphs}
\title{Generate graphs from peptide ratio table, using an edgelist calculated
on the fasta file.}
\usage{
generateQuantGraphs(
  exp_peptide_ratios,
  fasta_edgelist,
  seq_column = "Sequence",
  outpath = NULL,
  collProtNodes = TRUE,
  collPeptNodes = FALSE,
  suffix = ""
)
}
\arguments{
\item{exp_peptide_ratios}{\strong{SummarizedExperiment} \cr
A SummarizedExperiment from
\link{calculatePeptideRatios} with peptide
ratios.}

\item{fasta_edgelist}{\strong{data.frame} \cr
An edgelist created from the corresponding
FASTA file, eg. created with
\code{\link[=digestFASTA]{digestFASTA()}}.}

\item{seq_column}{\strong{character} \cr
The column name of the peptide sequence.}

\item{outpath}{\strong{character} \cr
The output path for the results.}

\item{collProtNodes}{\strong{logical} \cr
If \code{TRUE}, the protein nodes will
be collapsed.}

\item{collPeptNodes}{\strong{logical} \cr
If \code{TRUE}, the peptide nodes will
be collapsed.}

\item{suffix}{\strong{character} \cr
The suffix for saving results.}
}
\value{
A list of list of subgraphs
}
\description{
Generate graphs from peptide ratio table, using an edgelist calculated
on the fasta file.
}
\examples{
library(seqinr)
file <- system.file("extdata", "uniprot_proteome_Scerevisiae_filtered.fasta", package = "bppg")
fasta <- seqinr::read.fasta(file = file, seqtype = "AA", as.string = TRUE)
edgelist <- digestFASTA(fasta)

file <- system.file("extdata", "peptides_filtered.txt", package = "bppg")
group <- factor(rep(1:9, each = 3))
D <- readMqPeptideTable(path = file, group = group, LFQ = TRUE, remove_contaminants = FALSE)

dAgg <- aggregateReplicates(D)
exp_peptide_ratios <- calculatePeptideRatios(dAgg)

res <- generateQuantGraphs(exp_peptide_ratios, edgelist)
}
\seealso{
\code{\link[=digestFASTA]{digestFASTA()}}
}

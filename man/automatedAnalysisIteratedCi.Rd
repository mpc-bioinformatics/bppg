% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_optimization.R
\name{automatedAnalysisIteratedCi}
\alias{automatedAnalysisIteratedCi}
\title{Extract protein ratio solutions from the result of iterateOverCi()}
\usage{
automatedAnalysisIteratedCi(
  G,
  res,
  use_results_from_other_proteins = TRUE,
  verbose = FALSE,
  job = NULL,
  error_tol = 1e-10,
  ratioLog_tol = 1e-06
)
}
\arguments{
\item{G}{\strong{igraph object} \cr
An igraph graph of the bipartite
peptide-protein graph with peptide
ratios}

\item{res}{\strong{data.frame} \cr
The data.frame resulting from the
\code{\link[=iterateOverCi]{iterateOverCi()}} function.}

\item{use_results_from_other_proteins}{\strong{logical} \cr
If \code{TRUE}, the results from
other proteins within the same graph
will be used to calculate the
optimal solution for each protein
node. Default is TRUE}

\item{verbose}{\strong{logical} \cr
If \code{TRUE}, additional information
will be printed.}

\item{job}{\strong{BatchExperiment job object} \cr
Is used to print the job id and
parameters in the output}

\item{error_tol}{\strong{numeric(1)} \cr
Tolerance for a constant error term.
Default is 1e-10.}

\item{ratioLog_tol}{\strong{numeric(1)} \cr
Tolerance for a constant log2 protein
ratios. The default is 1e-6.}
}
\value{
A SummarizedExperiment object with one row for each protein in the
assay.
}
\description{
Extract protein ratio solutions from the result of iterateOverCi()
}
\examples{
file <- system.file("extdata", "quantGraphs.rds", package = "bppg")
graphs <- readRDS(file)
G <- graphs$"1_2"[[2]]
# small example with a small grid size
res <- iterateOverCi(G, gridSize = 100)
automatedAnalysisIteratedCi(G, res)
}
\seealso{
\code{\link[=iterateOverCi]{iterateOverCi()}}, \code{\link[=.minimizeSquaredError]{.minimizeSquaredError()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_FASTAPreprocessing.R
\name{digestFASTA}
\alias{digestFASTA}
\title{In silico tryptic digestion of whole FASTA file.}
\usage{
digestFASTA(
  fasta,
  missed_cleavages = 2,
  min_aa = 6,
  max_aa = 50,
  protOrigin = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{fasta}{\strong{list of character sequence} \cr
A fasta file, already read into R by
\code{\link[seqinr:read.fasta]{seqinr::read.fasta()}}. If several protein origins
are used they should be combined into one flatten
list and a list with the corresponding origins
should be provided for \strong{protOrigin}.}

\item{missed_cleavages}{\strong{integer} \cr
The maximal number of missed cleavages.}

\item{min_aa}{\strong{integer} \cr
The minimal number of amino acids
(set to 0 for no filtering).}

\item{max_aa}{\strong{integer} \cr
The maximal number of amino acids
(set to Inf for no filtering).}

\item{protOrigin}{\strong{list} \cr
A list with the protein origin corresponding to
\link[seqinr:fasta]{fasta}, proteins are used as index.}

\item{verbose}{\strong{logical} \cr
If \code{TRUE}, additional information on
each iteration of the digestion is
printed.}

\item{...}{
  Arguments passed on to \code{\link{.digest2}}
  \describe{
    \item{\code{sequence}}{\strong{character} \cr
The protein sequence.}
    \item{\code{enzyme}}{\strong{character} \cr
The enzyme used in digestion e.g. "trypsin" (does
not cut before proline).}
    \item{\code{missed}}{\strong{character} \cr
The maximal number of missed cleavages.}
    \item{\code{warn}}{\strong{logical} \cr
If \code{TRUE}, warnings will be printed e.g. if a
protein has no cleavage site.}
    \item{\code{remove_initial_M}}{\strong{logical} \cr
If \code{TRUE}, the initial methionine will be
removed from the peptides.}
  }}
}
\value{
data.frame with proteins and their peptide sequences, filtered
for minimal and maximal number of amino acids.
}
\description{
In silico tryptic digestion of whole FASTA file.
}
\examples{
library(seqinr)
file <- system.file("extdata", "uniprot_proteome_Scerevisiae_filtered.fasta",
    package = "bppg")
fasta <- seqinr::read.fasta(file = file, seqtype = "AA", as.string = TRUE)
res <- digestFASTA(fasta)

}
\seealso{
\code{\link[=.digest2]{.digest2()}}
}
